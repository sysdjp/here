
import 'package:flutter/material.dart';

class Page_Update extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: Text('Result'),
        ),
        body: Container(
          height: double.infinity,
          color: Colors.red,
        ),

      );
    // TODO: implement build
    throw UnimplementedError();
  }

}

